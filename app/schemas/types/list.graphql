type Query {
    list(id:String): List,
    lists: [List]
}
type List {
    id: String
    title: String,
    description: String,
    todos: [Todo]
}

type Mutation{
    createList(data: CreateListInput!): List!
}

input CreateListInput {
    title: String!,
    description: String,
}

